import React from "react";

const Contact = () => {
  return (
    <div className="nes-container is-rounded" id="contactContainer">
      <h1>Contact</h1>
      <p className="nes-balloon nes-pointer">
        I build things that ship and stay shipped. If you're working on
        something interesting — or need someone who can own a feature end to
        end — reach out.
      </p>
    </div>
  );
};

export default Contact;
